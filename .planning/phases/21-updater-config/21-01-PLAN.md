---
phase: 21-updater-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/tauri.conf.json
autonomous: true
requirements:
  - UCFG-01
  - UCFG-02

must_haves:
  truths:
    - "The `dialog` key does not exist in `plugins.updater` — the v1 remnant is fully removed"
    - "The `createUpdaterArtifacts` value is the boolean `true`, not the string `v1Compatible`"
    - "All existing updater config (active, endpoints, pubkey) remains intact"
    - "Capabilities file still contains updater:default, updater:allow-check, and process:allow-restart"
  artifacts:
    - path: "src-tauri/tauri.conf.json"
      provides: "Clean Tauri v2 updater configuration"
      contains: "\"createUpdaterArtifacts\": true"
  key_links:
    - from: "src-tauri/tauri.conf.json"
      to: "src-tauri/capabilities/default.json"
      via: "updater plugin permissions"
      pattern: "updater:default"
---

<objective>
Remove stale Tauri v1 updater config and enable v2-native updater artifacts.

Purpose: Unblock the JS updater API (`check()`) for Phase 22 by cleaning two config values — removing the non-existent `dialog` key (v1 remnant) and switching `createUpdaterArtifacts` from `"v1Compatible"` to `true` (v2 native format).

Output: A clean `tauri.conf.json` with v2-correct updater configuration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-updater-config/21-RESEARCH.md
@src-tauri/tauri.conf.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dialog key and set createUpdaterArtifacts to true</name>
  <files>src-tauri/tauri.conf.json</files>
  <action>
Edit `src-tauri/tauri.conf.json` to make exactly two changes:

1. **Remove the `"dialog": true` line** from `plugins.updater` — this key does not exist in the Tauri v2 updater Config struct. It was a v1 concept removed in v2. Do NOT set it to `false`; delete the entire key-value pair. The v2 JS API `check()` works unconditionally without this key.

2. **Change `"createUpdaterArtifacts"` from `"v1Compatible"` to `true`** (boolean, not string) in the `bundle` section. This switches from v1-compatible wrapped archives to v2-native updater artifacts. The `"v1Compatible"` value will be removed in Tauri v3.

Preserve all other config exactly as-is: `active`, `endpoints`, `pubkey`, bundle icons, resources, app windows, security CSP. Ensure valid JSON after edits (no trailing commas, correct structure).

Note: The capabilities file (`src-tauri/capabilities/default.json`) already contains `updater:default`, `updater:allow-check`, and `process:allow-restart`. No changes needed there — but verify it has not been accidentally modified.
  </action>
  <verify>
    <automated>python3 -c "import json,sys; c=json.load(open('src-tauri/tauri.conf.json')); sys.exit(0 if 'dialog' not in c['plugins']['updater'] and c['bundle']['createUpdaterArtifacts'] is True else 1)"</automated>
  </verify>
  <done>
- `plugins.updater` contains exactly: `active`, `endpoints`, `pubkey` — no `dialog` key
- `bundle.createUpdaterArtifacts` is the boolean `true`
- JSON is valid (parseable without errors)
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify config compiles and capabilities intact</name>
  <files>src-tauri/tauri.conf.json</files>
  <action>
Run `cargo check` in the `src-tauri` directory to confirm the modified config is accepted by the Tauri build system without errors or warnings about unknown config fields.

Also verify that `src-tauri/capabilities/default.json` still contains the three required permissions:
- `updater:default`
- `updater:allow-check`
- `process:allow-restart`

Use grep to confirm all three are present. If any are missing, add them back (they should already be there per research).
  </action>
  <verify>
    <automated>cd src-tauri && cargo check 2>&1 | grep -c "error" | grep -q "^0$" && grep -c "updater:default\|updater:allow-check\|process:allow-restart" capabilities/default.json | grep -q "3"</automated>
  </verify>
  <done>
- `cargo check` completes with zero errors
- capabilities/default.json contains all three required permissions
- Config is ready for Phase 22 JS API usage
  </done>
</task>

</tasks>

<verification>
1. Config inspection passes: `python3 -c "import json,sys; c=json.load(open('src-tauri/tauri.conf.json')); sys.exit(0 if 'dialog' not in c['plugins']['updater'] and c['bundle']['createUpdaterArtifacts'] is True else 1)"`
2. `cd src-tauri && cargo check` completes without errors
3. `grep -c "updater:default\|updater:allow-check\|process:allow-restart" src-tauri/capabilities/default.json` returns 3
</verification>

<success_criteria>
- tauri.conf.json has no `dialog` key in `plugins.updater`
- tauri.conf.json has `"createUpdaterArtifacts": true` (boolean) in `bundle`
- All existing config values preserved (endpoints, pubkey, active, icons, CSP)
- Capabilities permissions unchanged
- `cargo check` passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/21-updater-config/21-01-SUMMARY.md`
</output>
