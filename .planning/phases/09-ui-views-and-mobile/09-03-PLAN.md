---
phase: 09-ui-views-and-mobile
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/mobile.css
  - mobile/index.html
autonomous: false
requirements:
  - UIUX-10
must_haves:
  truths:
    - "Mobile PWA background uses Spotify warm black (#121212) matching the desktop midnight theme"
    - "Mobile PWA secondary background, tertiary background, and surface colors match desktop globals.css midnight theme values"
    - "Mobile PWA text colors are pure white and Spotify secondary (#b3b3b3), not the old blue-tinted values"
    - "Mobile PWA border color is neutral (#333333), not blue-tinted (#2a2a3a)"
    - "Mobile PWA success color is Spotify green (#1ed760)"
    - "Mobile PWA body font includes Inter as the preferred font family"
    - "Mobile PWA theme-color meta tag is updated to #121212"
    - "Mobile PWA functionality is unchanged — browse, search, and stream still work"
  artifacts:
    - path: "mobile/mobile.css"
      provides: "Updated CSS token values matching desktop midnight theme"
      contains: "--bg-primary: #121212, --text-primary: #ffffff, --success: #1ed760"
    - path: "mobile/index.html"
      provides: "Updated theme-color meta tag"
      contains: "theme-color.*#121212"
  key_links:
    - from: "mobile/mobile.css :root"
      to: "src/styles/globals.css [data-theme='midnight']"
      via: "CSS variable values must match"
      pattern: "--bg-primary.*#121212"
---

<objective>
Align the mobile PWA CSS design tokens with the desktop midnight theme established in Phase 7. Update the 8 CSS variables in mobile/mobile.css from blue-tinted dark values to Spotify warm blacks, and update the theme-color meta tag in index.html.

Purpose: The mobile PWA companion visually diverges from the redesigned desktop app because it retains the old pre-Phase-7 blue-tinted color scheme. This is a targeted token update — no interaction changes.
Output: Updated mobile/mobile.css and mobile/index.html with matching Spotify warm black tokens.
</objective>

<execution_context>
@/Users/nemanjamarjanovic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nemanjamarjanovic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ui-views-and-mobile/09-CONTEXT.md
@.planning/phases/09-ui-views-and-mobile/09-RESEARCH.md

<interfaces>
<!-- Current vs target values — extracted directly from mobile/mobile.css and src/styles/globals.css -->

Current mobile/mobile.css :root (OLD — blue-tinted):
```css
:root {
  --accent: #6366f1;          /* keep — same */
  --bg-primary: #0a0a0f;      /* OLD blue-tinted */
  --bg-secondary: #12121a;    /* OLD blue-tinted */
  --bg-tertiary: #1a1a28;     /* OLD blue-tinted */
  --text-primary: #e0e0e8;    /* OLD slightly blue-white */
  --text-secondary: #8888a0;  /* OLD blue-gray */
  --border: #2a2a3a;          /* OLD blue-tinted */
  --surface: #16161f;         /* OLD blue-tinted */
  --danger: #ef4444;          /* slight change */
  --success: #10b981;         /* OLD green */
}
```

Target values (from src/styles/globals.css midnight theme):
```css
:root {
  --accent: #6366f1;          /* unchanged */
  --bg-primary: #121212;      /* Spotify warm black */
  --bg-secondary: #181818;    /* Spotify warm black */
  --bg-tertiary: #282828;     /* Spotify warm black */
  --text-primary: #ffffff;    /* pure white */
  --text-secondary: #b3b3b3;  /* Spotify secondary */
  --border: #333333;          /* neutral border */
  --surface: #1e1e1e;         /* warm surface */
  --danger: #e53e3e;          /* match desktop */
  --success: #1ed760;         /* Spotify green */
}
```

Current mobile/index.html theme-color:
```html
<meta name="theme-color" content="#0a0a0f">
```

Target:
```html
<meta name="theme-color" content="#121212">
```

Current body font-family in mobile/mobile.css:
```css
body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
}
```

Target (add Inter as preferred, keep fallbacks):
```css
body {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
}
```

Google Fonts import to add to mobile/index.html (in <head>, before main.tsx):
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update mobile CSS token values and add Inter font</name>
  <files>mobile/mobile.css, mobile/index.html</files>
  <action>
**mobile/mobile.css changes:**

1. Update the `:root` block — replace all 10 CSS variable values:
```css
:root {
  --accent: #6366f1;
  --bg-primary: #121212;
  --bg-secondary: #181818;
  --bg-tertiary: #282828;
  --text-primary: #ffffff;
  --text-secondary: #b3b3b3;
  --border: #333333;
  --surface: #1e1e1e;
  --danger: #e53e3e;
  --success: #1ed760;
}
```

2. Update the `body` font-family declaration:
```css
body {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  /* other body rules unchanged */
}
```

Do NOT change any other CSS rules in mobile.css — only the :root block values and the body font-family. The entire rest of the file (all class definitions, layout rules, component styles) stays exactly as-is.

**mobile/index.html changes:**

1. Update the `theme-color` meta tag:
```html
<meta name="theme-color" content="#121212">
```

2. Add Google Fonts preconnect and Inter stylesheet links in `<head>`, immediately before the closing `</head>` tag (or before `<title>` — either position works):
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
```

Do NOT change the viewport meta, apple-mobile-web-app-* metas, manifest link, title, or script tag.

**Note on Inter offline:** The Google Fonts link requires internet access. If the mobile device is offline, the `-apple-system` fallback applies gracefully — visually acceptable per CONTEXT.md scope decision. Do not add a build step or local font copy.
  </action>
  <verify>
    <automated>npm run test -- --run</automated>
  </verify>
  <done>mobile/mobile.css :root has Spotify warm black values; body font includes Inter; mobile/index.html theme-color is #121212; Google Fonts links added; test suite green (mobile changes don't affect Vitest)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Visual verification of mobile PWA token alignment</name>
  <action>Human visual verification — no automated action. Review the how-to-verify steps below using the mobile PWA or browser DevTools.</action>
  <what-built>Updated mobile PWA CSS tokens and Inter font to match desktop midnight theme. Background is now Spotify warm black (#121212) instead of blue-tinted (#0a0a0f).</what-built>
  <how-to-verify>
    1. Build the mobile PWA: `cd mobile && npm run build` (or check if there's a build script in the root)
    2. Start the Tauri app: `npm run tauri dev`
    3. In the desktop app, share a playlist (Settings or playlist context menu -> Share) to get the companion URL
    4. Open the companion URL in a mobile browser or desktop browser to preview mobile PWA
    5. Confirm: background is warm black (not blue-tinted) — should look like #121212 (identical to desktop)
    6. Confirm: text is pure white (not the old slightly-blue #e0e0e8)
    7. Confirm: accent buttons/highlights are the same indigo (#6366f1)
    8. Confirm: all PWA functionality still works — browse library, play tracks, search
    Alternatively: inspect the served mobile/mobile.css via browser DevTools to verify the CSS variable values match the target without launching a mobile device.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
After task:
1. `npm run test -- --run` passes (not affected by mobile CSS changes)
2. grep confirms new values: `grep "bg-primary" mobile/mobile.css` → `#121212`
3. grep confirms: `grep "theme-color" mobile/index.html` → `#121212`
4. grep confirms: `grep "Inter" mobile/mobile.css` → present in font-family
</verification>

<success_criteria>
- mobile/mobile.css :root uses Spotify warm black values matching globals.css midnight theme
- mobile/index.html theme-color is #121212
- Inter font declared in body font-family with Google Fonts import
- Mobile PWA functionality unchanged (no interaction code modified)
- `npm run test -- --run` exits green
- Human verification checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-views-and-mobile/09-03-SUMMARY.md`
</output>
