---
phase: 12-css-layout-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/TrackTable.css
autonomous: false
requirements:
  - LAYT-01
  - LAYT-02

must_haves:
  truths:
    - "Track table rows fill the full window width at 800px, 1280px, and 1920px — no blank gap to the right of the last column"
    - "Track table header background covers the full window width — it does not stop at the column content boundary"
    - "Horizontal scrollbar appears only when total column width exceeds the window width — not on wide displays"
  artifacts:
    - path: "src/components/TrackTable.css"
      provides: "min-width: 100% rule on .track-table-holder"
      contains: ".track-table-holder"
  key_links:
    - from: ".track-table-holder"
      to: ".track-table-scroll-area"
      via: "min-width: 100% causes holder to fill scroll container at any window width"
      pattern: "min-width:\\s*100%"
---

<objective>
Add a single CSS rule to make track table rows and header background extend to the full window width at any window size.

Purpose: Track table rows currently collapse to fit-content width (the sum of column widths). On wide displays, a visible whitespace gap appears to the right of the last column and the header background stops mid-screen. This violates LAYT-01 and LAYT-02.

Output: `src/components/TrackTable.css` with `min-width: 100%` on `.track-table-holder`. No JavaScript changes. No test scaffolding needed.
</objective>

<execution_context>
@/Users/nemanjamarjanovic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nemanjamarjanovic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-css-layout-fix/12-RESEARCH.md

<interfaces>
<!-- DOM hierarchy from TrackTable.tsx (no changes needed to this file) -->
<!--
.track-table-container          (flex col, height: 100%)
  .track-table-search           (flex-shrink: 0)
  .track-table-scroll-area      (flex: 1, overflow: auto)  ← parentRef for TanStack Virtual
    .track-table-holder         (NO width rule currently — shrinks to fit-content) ← THE FIX TARGET
      .track-table-header       (position: sticky; top: 0; width: 100%)
        .track-table-row        (display: flex; width: 100%; min-width: fit-content)
      .track-table-body         (position: relative; width: 100%)
        .track-table-row        (position: absolute; width: 100%; transform: translateY)
  .track-table-footer           (flex-shrink: 0)

Key: TanStack Virtual measures .track-table-scroll-area (parentRef), NOT .track-table-holder.
     Changing holder width does not affect virtualizer measurements.
-->

<!-- Current TrackTable.css state — .track-table-holder has NO CSS rule. -->
<!-- After fix: -->
<!--
.track-table-holder {
  min-width: 100%;
}
-->
<!-- Why min-width and not width:
  - width: 100% would prevent horizontal scrolling (holder pins to scroll container width)
  - min-width: 100% expands to fill scroll container when window is wide,
    but still allows holder to grow beyond 100% when columns exceed window width (triggers scrollbar)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add min-width: 100% to .track-table-holder</name>
  <files>src/components/TrackTable.css</files>
  <action>
Add a new CSS rule for `.track-table-holder` immediately after the `.track-table-scroll-area` rule block (or at a logical position in the scroll area / body section of the file).

The rule to add:

```css
.track-table-holder {
  min-width: 100%;
}
```

CRITICAL constraints:
- Add `min-width: 100%` — do NOT use `width: 100%` (that would prevent horizontal scroll)
- Do NOT add any width property to `.track-table-row` — breaks TanStack Virtual width measurement
- Do NOT add `overflow-x: hidden` anywhere — would clip content on narrow windows
- Do NOT modify TrackTable.tsx — this is CSS-only
- Do NOT use `100vw` — ignores sidebar width and causes app-shell overflow

After the change, run `npm test` to confirm no TypeScript/logic regressions.
  </action>
  <verify>
    <automated>cd /Users/nemanjamarjanovic/Desktop/Cursor/RecoDeck && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>TrackTable.css contains a `.track-table-holder { min-width: 100%; }` rule. `npm test` passes with no new failures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Added `min-width: 100%` to `.track-table-holder` in TrackTable.css. This single CSS rule causes track table rows and the header background to fill the full window width at any window size, while preserving horizontal scroll on narrow windows.</what-built>
  <how-to-verify>
Run the app (`npm run tauri dev` or use the already-running dev build), navigate to the All Tracks view (or any view showing the track table), then:

1. Resize the window to approximately 1920px wide (or maximize on a wide monitor) — confirm NO blank gap appears to the right of the last track table column, and the header background (`--bg-secondary`) covers the full width edge to edge.

2. Resize to approximately 1280px wide — same check: rows fill edge to edge, no gap, header background full width.

3. Resize to approximately 800px wide — same check.

4. Narrow the window to below ~700px (narrower than the total column width) — confirm a horizontal scrollbar appears and the columns are not clipped. The table should be scrollable horizontally.

Expected at steps 1-3: Track rows span the full window width (minus sidebar). Header background color extends to the window edge.
Expected at step 4: Horizontal scrollbar is visible. Content is not cut off.
  </how-to-verify>
  <resume-signal>Type "approved" if all four checks pass, or describe what you see if something looks wrong.</resume-signal>
</task>

</tasks>

<verification>
- `npm test` passes after the CSS change (no regressions)
- Visual: track rows fill full window width at 800px, 1280px, 1920px
- Visual: header background covers full scroll area width
- Visual: horizontal scroll activates only when window narrower than column content
</verification>

<success_criteria>
At any window width (800px, 1280px, 1920px):
- Track table rows fill the full width — no blank gap to the right of the last column (LAYT-01)
- Track table header background extends edge to edge — does not stop at the column content boundary (LAYT-02)
- Horizontal scrollbar appears only when columns overflow the window — not on wide displays
</success_criteria>

<output>
After completion, create `.planning/phases/12-css-layout-fix/12-01-SUMMARY.md` using the summary template.
</output>
